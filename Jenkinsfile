pipeline{

agent any

    stages{
        stage('Checkout SCM'){
            echo 'Downloading project code'
        }
        stage('Build'){
            echo 'Build project'
        }
        stage('Run API tests'){
            echo 'Running API tests'
        }
        stage('Run UI tests'){
            echo 'Running UI tests'
        }

}
}